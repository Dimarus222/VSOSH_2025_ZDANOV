<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MetalWorks Pro - Производство и продажа металлоконструкций</title>
    <meta name="description" content="Профессиональное производство металлоконструкций: фермы, колонны, балки, строительные леса. Доставка по всей России. Гарантия качества.">
    <meta name="keywords" content="металлоконструкции, фермы, балки, колонны, строительные леса, металлоизделия, производство металлоконструкций">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&family=Oswald:wght@500;600;700&display=swap">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        /* Основные стили и переменные */
        :root {
            --primary: #1e3a5f;
            --secondary: #c62828;
            --accent: #ff9800;
            --light: #f5f7fa;
            --dark: #0d1b2a;
            --gray: #8d99ae;
            --success: #2ecc71;
            --shadow: 0 4px 12px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Roboto', sans-serif;
        }

        body {
            background-color: #f8f9fa;
            color: #333;
            line-height: 1.6;
            overflow-x: hidden;
        }

        h1, h2, h3, h4, h5 {
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .btn {
            padding: 12px 24px;
            border-radius: 4px;
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
            text-align: center;
            display: inline-block;
            text-decoration: none;
            font-size: 16px;
            border: none;
        }

        .btn-primary {
            background: var(--secondary);
            color: white;
            box-shadow: 0 4px 8px rgba(198, 40, 40, 0.3);
        }

        .btn-primary:hover {
            background: #b71c1c;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(183, 28, 28, 0.4);
        }

        .btn-outline {
            background: transparent;
            border: 2px solid var(--primary);
            color: var(--primary);
        }

        .btn-outline:hover {
            background: var(--primary);
            color: white;
        }

        .section {
            padding: 80px 0;
        }

        .section-title {
            text-align: center;
            margin-bottom: 50px;
            font-size: 36px;
            color: var(--primary);
            position: relative;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 5px;
            background: var(--secondary);
            border-radius: 5px;
        }

        /* Шапка */
        header {
            background-color: var(--primary);
            color: white;
            padding: 15px 0;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: var(--shadow);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 24px;
            font-weight: 700;
        }

        .logo i {
            color: var(--accent);
            font-size: 28px;
        }

        .logo span {
            font-family: 'Oswald', sans-serif;
        }

        .search-bar {
            flex: 1;
            max-width: 500px;
            margin: 0 20px;
            position: relative;
        }

        .search-bar input {
            width: 100%;
            padding: 12px 20px;
            border-radius: 30px;
            border: none;
            font-size: 16px;
            box-shadow: var(--shadow);
        }

        .search-bar button {
            position: absolute;
            right: 5px;
            top: 5px;
            background: var(--secondary);
            color: white;
            border: none;
            border-radius: 50%;
            width: 36px;
            height: 36px;
            cursor: pointer;
        }

        .header-actions {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .header-action {
            display: flex;
            flex-direction: column;
            align-items: center;
            cursor: pointer;
            transition: var(--transition);
            padding: 5px 10px;
            border-radius: 4px;
        }

        .header-action:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .header-action i {
            font-size: 22px;
        }

        .header-action span {
            font-size: 14px;
            margin-top: 4px;
        }

        .cart-count {
            background: var(--accent);
            color: var(--dark);
            font-size: 12px;
            min-width: 20px;
            height: 20px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            position: absolute;
            top: -5px;
            right: -5px;
            font-weight: bold;
        }

        /* Навигация */
        nav {
            background: var(--dark);
            padding: 10px 0;
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 25px;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            transition: var(--transition);
            position: relative;
            padding: 8px 0;
            font-size: 16px;
        }

        .nav-links a:hover {
            color: var(--accent);
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 3px;
            background: var(--accent);
            transition: var(--transition);
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        /* Герой-секция */
        .hero {
            background: linear-gradient(rgba(13, 27, 42, 0.8), rgba(13, 27, 42, 0.9)), url('https://images.unsplash.com/photo-1518644730709-0835105d9daa?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80');
            background-size: cover;
            background-position: center;
            color: white;
            padding: 120px 0;
            text-align: center;
        }

        .hero h1 {
            font-size: 52px;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .hero p {
            font-size: 20px;
            max-width: 800px;
            margin: 0 auto 40px;
            font-weight: 300;
        }

        .hero-btns {
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        /* Категории */
        .categories {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
        }

        .category-card {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: var(--transition);
            position: relative;
        }

        .category-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
        }

        .category-img {
            height: 220px;
            overflow: hidden;
        }

        .category-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
        }

        .category-card:hover .category-img img {
            transform: scale(1.1);
        }

        .category-content {
            padding: 20px;
            text-align: center;
        }

        .category-content h3 {
            margin-bottom: 10px;
            color: var(--primary);
            font-size: 22px;
        }

        /* Фильтры и каталог */
        .catalog-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            flex-wrap: wrap;
            gap: 20px;
        }

        .filters-container {
            display: grid;
            grid-template-columns: 250px 1fr;
            gap: 30px;
        }

        .filters {
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: var(--shadow);
            height: fit-content;
        }

        .filter-group {
            margin-bottom: 25px;
            padding-bottom: 20px;
            border-bottom: 1px solid #eee;
        }

        .filter-group:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border: none;
        }

        .filter-title {
            font-size: 18px;
            margin-bottom: 15px;
            color: var(--primary);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .filter-options {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .filter-option {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .filter-option input {
            width: 18px;
            height: 18px;
        }

        .filter-option label {
            cursor: pointer;
        }

        .range-values {
            display: flex;
            justify-content: space-between;
            margin-top: 10px;
            font-size: 14px;
            color: var(--gray);
        }

        .catalog-products {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 30px;
        }

        .product-card {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: var(--transition);
            position: relative;
            display: flex;
            flex-direction: column;
            height: 100%;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }

        .product-badge {
            position: absolute;
            top: 15px;
            left: 15px;
            background: var(--secondary);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            z-index: 10;
        }

        .product-img {
            height: 220px;
            overflow: hidden;
            position: relative;
        }

        .product-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
        }

        .product-card:hover .product-img img {
            transform: scale(1.05);
        }

        .product-content {
            padding: 20px;
            display: flex;
            flex-direction: column;
            flex-grow: 1;
        }

        .product-title {
            font-size: 18px;
            margin-bottom: 10px;
            color: var(--primary);
            flex-grow: 1;
        }

        .product-meta {
            display: flex;
            justify-content: space-between;
            margin-bottom: 15px;
            font-size: 14px;
            color: var(--gray);
        }

        .product-price {
            font-size: 22px;
            font-weight: 700;
            color: var(--secondary);
            margin-bottom: 15px;
        }

        .product-actions {
            display: flex;
            gap: 10px;
            margin-top: auto;
        }

        .btn-add-to-cart {
            flex: 1;
            background: var(--primary);
            color: white;
            padding: 10px;
            border-radius: 4px;
            cursor: pointer;
            transition: var(--transition);
            font-weight: 500;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn-add-to-cart:hover {
            background: #152a41;
        }

        .btn-view {
            background: transparent;
            border: 1px solid var(--primary);
            color: var(--primary);
            padding: 10px;
            border-radius: 4px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn-view:hover {
            background: var(--primary);
            color: white;
        }

        /* О нас */
        .about {
            background: var(--light);
        }

        .about-content {
            display: flex;
            align-items: center;
            gap: 50px;
        }

        .about-img {
            flex: 1;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: var(--shadow);
        }

        .about-img img {
            width: 100%;
            display: block;
        }

        .about-text {
            flex: 1;
        }

        .about-text h2 {
            color: var(--primary);
            margin-bottom: 20px;
            font-size: 36px;
        }

        .about-text p {
            margin-bottom: 20px;
            font-size: 18px;
            line-height: 1.8;
        }

        .features {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 25px;
            margin-top: 30px;
        }

        .feature {
            display: flex;
            gap: 15px;
        }

        .feature i {
            color: var(--accent);
            font-size: 24px;
            min-width: 40px;
            padding-top: 5px;
        }

        .feature h4 {
            font-size: 18px;
            margin-bottom: 5px;
            color: var(--primary);
        }

        /* Статистика */
        .stats {
            background: linear-gradient(to right, var(--primary), var(--dark));
            color: white;
            padding: 60px 0;
            text-align: center;
        }

        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }

        .stat-item {
            padding: 20px;
        }

        .stat-number {
            font-size: 48px;
            font-weight: 700;
            margin-bottom: 10px;
            color: var(--accent);
            font-family: 'Oswald', sans-serif;
        }

        .stat-text {
            font-size: 18px;
            font-weight: 300;
        }

        /* Футер */
        footer {
            background: var(--dark);
            color: white;
            padding: 70px 0 30px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 50px;
        }

        .footer-column h3 {
            font-size: 22px;
            margin-bottom: 25px;
            position: relative;
            padding-bottom: 15px;
        }

        .footer-column h3::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 50px;
            height: 3px;
            background: var(--accent);
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 15px;
        }

        .footer-links a {
            color: #a8b2d1;
            text-decoration: none;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .footer-links a:hover {
            color: var(--accent);
            padding-left: 5px;
        }

        .footer-contact p {
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .social-icons {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .social-icons a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 45px;
            height: 45px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            color: white;
            transition: var(--transition);
            font-size: 18px;
        }

        .social-icons a:hover {
            background: var(--accent);
            transform: translateY(-3px);
        }

        .copyright {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: #a8b2d1;
            font-size: 16px;
        }

        /* Модальные окна */
        .modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 2000;
            opacity: 0;
            visibility: hidden;
            transition: var(--transition);
        }

        .modal.active {
            opacity: 1;
            visibility: visible;
        }

        .modal-content {
            background: white;
            border-radius: 10px;
            width: 90%;
            max-width: 500px;
            position: relative;
            transform: translateY(-50px);
            transition: var(--transition);
            max-height: 90vh;
            overflow-y: auto;
        }

        .modal.active .modal-content {
            transform: translateY(0);
        }

        .modal-header {
            padding: 20px;
            background: var(--primary);
            color: white;
            border-radius: 10px 10px 0 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 10;
        }

        .modal-header h2 {
            font-size: 24px;
        }

        .close-modal {
            background: transparent;
            border: none;
            color: white;
            font-size: 28px;
            cursor: pointer;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            transition: var(--transition);
        }

        .close-modal:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .modal-body {
            padding: 30px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: var(--dark);
        }

        .form-control {
            width: 100%;
            padding: 14px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 16px;
            transition: var(--transition);
        }

        .form-control:focus {
            border-color: var(--accent);
            outline: none;
            box-shadow: 0 0 0 3px rgba(255, 152, 0, 0.2);
        }

        .form-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 20px;
        }

        .form-footer a {
            color: var(--accent);
            text-decoration: none;
            font-weight: 500;
        }

        .form-footer a:hover {
            text-decoration: underline;
        }

        /* Корзина */
        .cart-modal {
            max-width: 800px;
        }

        .cart-items {
            max-height: 400px;
            overflow-y: auto;
            padding: 10px;
        }

        .cart-item {
            display: flex;
            padding: 20px 0;
            border-bottom: 1px solid #eee;
        }

        .cart-item-img {
            width: 100px;
            height: 100px;
            border-radius: 4px;
            overflow: hidden;
            margin-right: 20px;
            flex-shrink: 0;
        }

        .cart-item-img img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .cart-item-details {
            flex: 1;
        }

        .cart-item-title {
            font-weight: 600;
            margin-bottom: 8px;
            font-size: 18px;
            color: var(--primary);
        }

        .cart-item-price {
            color: var(--secondary);
            font-weight: 700;
            margin-bottom: 10px;
            font-size: 18px;
        }

        .cart-item-actions {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .quantity-control {
            display: flex;
            align-items: center;
            border: 1px solid #ddd;
            border-radius: 4px;
            overflow: hidden;
        }

        .quantity-btn {
            background: #f8f9fa;
            border: none;
            width: 36px;
            height: 36px;
            cursor: pointer;
            font-size: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .quantity-input {
            width: 50px;
            height: 36px;
            text-align: center;
            border: none;
            border-left: 1px solid #ddd;
            border-right: 1px solid #ddd;
            font-size: 16px;
        }

        .remove-item {
            background: transparent;
            border: none;
            color: var(--secondary);
            cursor: pointer;
            margin-left: 10px;
            font-size: 18px;
        }

        .cart-total {
            padding: 20px;
            display: flex;
            justify-content: space-between;
            font-size: 22px;
            font-weight: 700;
            background: #f8f9fa;
            border-radius: 0 0 10px 10px;
            border-top: 1px solid #eee;
            position: sticky;
            bottom: 0;
        }

        .cart-actions {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
            gap: 15px;
        }

        /* Форма заявки */
        .request-form {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: var(--shadow);
            max-width: 600px;
            margin: 30px auto;
        }

        .form-title {
            text-align: center;
            margin-bottom: 30px;
            color: var(--primary);
            font-size: 28px;
        }

        .form-row {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
        }

        .form-row .form-group {
            flex: 1;
            margin-bottom: 0;
        }

        /* Адаптивность */
        @media (max-width: 992px) {
            .about-content {
                flex-direction: column;
            }
            
            .search-bar {
                order: 3;
                max-width: 100%;
                margin: 15px 0 0;
            }
            
            .filters-container {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 36px;
            }
            
            .hero p {
                font-size: 18px;
            }
            
            .nav-links {
                display: none;
            }
            
            .hero-btns {
                flex-direction: column;
                gap: 15px;
            }
            
            .section {
                padding: 60px 0;
            }
            
            .section-title {
                font-size: 28px;
            }
            
            .form-row {
                flex-direction: column;
                gap: 0;
            }
        }

        @media (max-width: 576px) {
            .header-actions {
                gap: 10px;
            }
            
            .header-action span {
                display: none;
            }
            
            .hero {
                padding: 80px 0;
            }
            
            .cart-actions {
                flex-direction: column;
            }
            
            .cart-item {
                flex-direction: column;
            }
            
            .cart-item-img {
                width: 100%;
                height: 150px;
                margin-right: 0;
                margin-bottom: 15px;
            }
        }
    </style>
</head>
<body>
    <!-- Шапка сайта -->
    <header>
        <div class="container header-content">
            <div class="logo">
                <i class="fas fa-industry"></i>
                <span>METALWORKS PRO</span>
            </div>
            
            <div class="search-bar">
                <input type="text" placeholder="Поиск металлоконструкций...">
                <button><i class="fas fa-search"></i></button>
            </div>
            
            <div class="header-actions">
                <div class="header-action" id="login-btn">
                    <i class="fas fa-user"></i>
                    <span>Войти</span>
                </div>
                <div class="header-action" id="request-btn">
                    <i class="fas fa-file-alt"></i>
                    <span>Заявка</span>
                </div>
                <div class="header-action" id="cart-btn" style="position: relative;">
                    <i class="fas fa-shopping-cart"></i>
                    <span>Корзина</span>
                    <div class="cart-count">0</div>
                </div>
            </div>
        </div>
    </header>
    
    <!-- Навигация -->
    <nav>
        <div class="container nav-container">
            <ul class="nav-links">
                <li><a href="#" class="active">Главная</a></li>
                <li><a href="#">Каталог</a></li>
                <li><a href="#">Производство</a></li>
                <li><a href="#">Проекты</a></li>
                <li><a href="#">Доставка</a></li>
                <li><a href="#">Контакты</a></li>
            </ul>
            <div class="header-actions">
                <a href="tel:+74951234567" style="color: white; text-decoration: none;">
                    <i class="fas fa-phone-alt"></i> +7 (495) 123-45-67
                </a>
            </div>
        </div>
    </nav>
    
    <!-- Герой-секция -->
    <section class="hero">
        <div class="container">
            <h1>Профессиональные металлоконструкции</h1>
            <p>Производство и монтаж металлических конструкций любой сложности. Гарантия качества, индивидуальный подход, доставка по всей России.</p>
            <div class="hero-btns">
                <a href="#catalog" class="btn btn-primary">Перейти в каталог</a>
                <a href="#request" class="btn btn-outline">Оставить заявку</a>
            </div>
        </div>
    </section>
    
    <!-- Категории -->
    <section class="section">
        <div class="container">
            <h2 class="section-title">Основные категории</h2>
            <div class="categories">
                <div class="category-card">
                    <div class="category-img">
                        <img src="https://images.unsplash.com/photo-1560448204-603b3fc33ddc?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80" alt="Металлические фермы">
                    </div>
                    <div class="category-content">
                        <h3>Металлические фермы</h3>
                        <p>Для промышленных зданий и сооружений</p>
                    </div>
                </div>
                
                <div class="category-card">
                    <div class="category-img">
                        <img src="https://images.unsplash.com/photo-1518998053908-8cc8e31524ae?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1169&q=80" alt="Опорные конструкции">
                    </div>
                    <div class="category-content">
                        <h3>Опорные конструкции</h3>
                        <p>Для ЛЭП, мачт и антенных сооружений</p>
                    </div>
                </div>
                
                <div class="category-card">
                    <div class="category-img">
                        <img src="https://images.unsplash.com/photo-1581091226033-d5c48150dbaa?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80" alt="Строительные леса">
                    </div>
                    <div class="category-content">
                        <h3>Строительные леса</h3>
                        <p>Для фасадных и отделочных работ</p>
                    </div>
                </div>
                
                <div class="category-card">
                    <div class="category-img">
                        <img src="https://images.unsplash.com/photo-1620712943543-bcc4688e7485?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=765&q=80" alt="Индивидуальные проекты">
                    </div>
                    <div class="category-content">
                        <h3>Индивидуальные проекты</h3>
                        <p>По вашим чертежам и спецификациям</p>
                    </div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- Каталог продукции -->
    <section class="section" id="catalog">
        <div class="container">
            <div class="catalog-header">
                <h2 class="section-title">Каталог продукции</h2>
                <div>
                    <span>Сортировка: </span>
                    <select id="sort-products" style="padding: 10px; border-radius: 4px;">
                        <option value="default">По умолчанию</option>
                        <option value="price-asc">Цена по возрастанию</option>
                        <option value="price-desc">Цена по убыванию</option>
                        <option value="popular">По популярности</option>
                    </select>
                </div>
            </div>
            
            <div class="filters-container">
                <div class="filters">
                    <div class="filter-group">
                        <div class="filter-title">
                            <span>Категории</span>
                        </div>
                        <div class="filter-options" id="category-filters">
                            <!-- Динамически заполняется -->
                        </div>
                    </div>
                    
                    <div class="filter-group">
                        <div class="filter-title">
                            <span>Цена, ₽</span>
                        </div>
                        <input type="range" min="0" max="500000" value="250000" class="slider" id="price-range">
                        <div class="range-values">
                            <span>0</span>
                            <span>250 000</span>
                            <span>500 000+</span>
                        </div>
                    </div>
                    
                    <div class="filter-group">
                        <div class="filter-title">
                            <span>Материал</span>
                        </div>
                        <div class="filter-options" id="material-filters">
                            <!-- Динамически заполняется -->
                        </div>
                    </div>
                    
                    <div class="filter-group">
                        <div class="filter-title">
                            <span>Наличие</span>
                        </div>
                        <div class="filter-options">
                            <div class="filter-option">
                                <input type="checkbox" id="in-stock" checked>
                                <label for="in-stock">В наличии</label>
                            </div>
                            <div class="filter-option">
                                <input type="checkbox" id="pre-order">
                                <label for="pre-order">Под заказ</label>
                            </div>
                        </div>
                    </div>
                    
                    <button class="btn btn-primary" style="width: 100%; margin-top: 20px;" id="apply-filters">Применить фильтры</button>
                </div>
                
                <div>
                    <div class="catalog-products" id="products-container">
                        <!-- Продукты загружаются динамически -->
                    </div>
                    
                    <div class="pagination" id="pagination">
                        <!-- Пагинация генерируется динамически -->
                    </div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- О нас -->
    <section class="section about">
        <div class="container">
            <h2 class="section-title">О нашей компании</h2>
            <div class="about-content">
                <div class="about-img">
                    <img src="https://images.unsplash.com/photo-1603739903239-8b6e64c3b185?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80" alt="Наше производство">
                </div>
                <div class="about-text">
                    <h2>MetalWorks Pro - качественные металлоконструкции с 2005 года</h2>
                    <p>Мы специализируемся на производстве металлоконструкций любой сложности для промышленного и гражданского строительства. Наше предприятие оснащено современным оборудованием, что позволяет нам гарантировать высочайшее качество продукции.</p>
                    <p>Наши специалисты имеют многолетний опыт в проектировании и изготовлении металлоконструкций, что позволяет нам выполнять самые сложные задачи в кратчайшие сроки.</p>
                    
                    <div class="features">
                        <div class="feature">
                            <i class="fas fa-check-circle"></i>
                            <div>
                                <h4>Качество</h4>
                                <p>Строгий контроль на всех этапах</p>
                            </div>
                        </div>
                        
                        <div class="feature">
                            <i class="fas fa-shipping-fast"></i>
                            <div>
                                <h4>Доставка</h4>
                                <p>По всей России и СНГ</p>
                            </div>
                        </div>
                        
                        <div class="feature">
                            <i class="fas fa-ruler-combined"></i>
                            <div>
                                <h4>Индивидуальный подход</h4>
                                <p>Изготовление по вашим чертежам</p>
                            </div>
                        </div>
                        
                        <div class="feature">
                            <i class="fas fa-award"></i>
                            <div>
                                <h4>Гарантия</h4>
                                <p>5 лет на все конструкции</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- Статистика -->
    <section class="stats">
        <div class="container stats-container">
            <div class="stat-item">
                <div class="stat-number">18+</div>
                <div class="stat-text">Лет на рынке</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">200+</div>
                <div class="stat-text">Видов продукции</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">3500+</div>
                <div class="stat-text">Выполненных заказов</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">98%</div>
                <div class="stat-text">Довольных клиентов</div>
            </div>
        </div>
    </section>
    
    <!-- Форма заявки -->
    <section class="section" id="request">
        <div class="container">
            <h2 class="section-title">Оставьте заявку на расчет</h2>
            <div class="request-form">
                <h3 class="form-title">Рассчитаем стоимость за 24 часа</h3>
                <form id="request-form">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="name">Ваше имя</label>
                            <input type="text" id="name" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label for="phone">Телефон</label>
                            <input type="tel" id="phone" class="form-control" required>
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" class="form-control">
                    </div>
                    
                    <div class="form-group">
                        <label for="category">Категория продукции</label>
                        <select id="category" class="form-control">
                            <option value="">Выберите категорию</option>
                            <option value="ferma">Металлические фермы</option>
                            <option value="kolonna">Колонны</option>
                            <option value="balka">Балки</option>
                            <option value="lesa">Строительные леса</option>
                            <option value="individual">Индивидуальный проект</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="message">Детали заказа</label>
                        <textarea id="message" class="form-control" rows="5" placeholder="Опишите необходимые изделия, размеры, количество и другие требования"></textarea>
                    </div>
                    
                    <div class="form-group">
                        <label for="file">Прикрепить файл (чертежи, спецификации)</label>
                        <input type="file" id="file" class="form-control">
                    </div>
                    
                    <button type="submit" class="btn btn-primary" style="width: 100%; padding: 15px; font-size: 18px;">Отправить заявку</button>
                </form>
            </div>
        </div>
    </section>
    
    <!-- Футер -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-column">
                    <h3>MetalWorks Pro</h3>
                    <p>Производство металлоконструкций для промышленного и гражданского строительства. Качество, надежность и индивидуальный подход.</p>
                    <div class="social-icons">
                        <a href="#"><i class="fab fa-vk"></i></a>
                        <a href="#"><i class="fab fa-telegram"></i></a>
                        <a href="#"><i class="fab fa-youtube"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                    </div>
                </div>
                
                <div class="footer-column">
                    <h3>Категории</h3>
                    <ul class="footer-links">
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Металлические фермы</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Опорные конструкции</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Строительные леса</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Каркасы зданий</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Индивидуальные проекты</a></li>
                    </ul>
                </div>
                
                <div class="footer-column">
                    <h3>Полезные ссылки</h3>
                    <ul class="footer-links">
                        <li><a href="#"><i class="fas fa-chevron-right"></i> О компании</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Производство</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Наши проекты</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Доставка и оплата</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Контакты</a></li>
                    </ul>
                </div>
                
                <div class="footer-column">
                    <h3>Контакты</h3>
                    <div class="footer-contact">
                        <p><i class="fas fa-map-marker-alt"></i> г. Москва, ул. Промышленная, 15</p>
                        <p><i class="fas fa-phone"></i> +7 (495) 123-45-67</p>
                        <p><i class="fas fa-envelope"></i> info@metalworkspro.ru</p>
                        <p><i class="fas fa-clock"></i> Пн-Пт: 9:00 - 18:00</p>
                    </div>
                </div>
            </div>
            
            <div class="copyright">
                <p>&copy; 2023 MetalWorks Pro. Все права защищены.</p>
            </div>
        </div>
    </footer>
    
    <!-- Модальные окна -->
    <div class="modal" id="login-modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2>Авторизация</h2>
                <button class="close-modal">&times;</button>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" class="form-control" placeholder="Введите ваш email">
                </div>
                
                <div class="form-group">
                    <label for="password">Пароль</label>
                    <input type="password" id="password" class="form-control" placeholder="Введите пароль">
                </div>
                
                <button class="btn btn-primary" style="width:100%">Войти</button>
                
                <div class="form-footer">
                    <a href="#">Забыли пароль?</a>
                    <a href="#">Регистрация</a>
                </div>
            </div>
        </div>
    </div>
    
    <div class="modal" id="cart-modal">
        <div class="modal-content cart-modal">
            <div class="modal-header">
                <h2>Ваша корзина</h2>
                <button class="close-modal">&times;</button>
            </div>
            <div class="modal-body">
                <div class="cart-items" id="cart-items">
                    <!-- Товары будут добавляться динамически -->
                </div>
                
                <div class="cart-total">
                    <span>Итого:</span>
                    <span id="cart-total-price">0 ₽</span>
                </div>
                
                <div class="cart-actions">
                    <button class="btn btn-outline">Продолжить покупки</button>
                    <button class="btn btn-primary">Оформить заказ</button>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // База данных товаров (более 200 позиций)
        const productsDB = [
            // Фермы (30 позиций)
            { id: 1, name: "Ферма арочная 12м", category: "Фермы", price: 28500, material: "Сталь", inStock: true, img: "https://images.unsplash.com/photo-1626804475297-41608ea09aeb?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80", description: "Арочная ферма пролетом 12 метров" },
            { id: 2, name: "Ферма арочная 15м", category: "Фермы", price: 34500, material: "Сталь", inStock: true, img: "https://example.com/ferma15m.jpg", description: "Арочная ферма пролетом 15 метров" },
            { id: 3, name: "Ферма треугольная 10м", category: "Фермы", price: 22500, material: "Сталь", inStock: true, img: "https://example.com/ferma_triangle.jpg", description: "Треугольная ферма пролетом 10 метров" },
            // ... 27 других ферм ...
            
            // Колонны (30 позиций)
            { id: 101, name: "Колонна стальная 6м", category: "Колонны", price: 12800, material: "Сталь", inStock: true, img: "https://images.unsplash.com/photo-1560448204-e02f11c3d0e2?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80", description: "Колонна стальная высотой 6 метров" },
            { id: 102, name: "Колонна стальная 8м", category: "Колонны", price: 15600, material: "Сталь", inStock: true, img: "https://example.com/col8m.jpg", description: "Колонна стальная высотой 8 метров" },
            { id: 103, name: "Колонна нержавеющая 5м", category: "Колонны", price: 24500, material: "Нержавейка", inStock: false, img: "https://example.com/col_nerg.jpg", description: "Колонна из нержавеющей стали высотой 5 метров" },
            // ... 27 других колонн ...
            
            // Балки (30 позиций)
            { id: 201, name: "Балка двутавровая 8м", category: "Балки", price: 23400, material: "Сталь", inStock: true, img: "https://images.unsplash.com/photo-1617103996702-96ff29b1c467?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1032&q=80", description: "Двутавровая балка длиной 8 метров" },
            { id: 202, name: "Балка швеллер 6м", category: "Балки", price: 18500, material: "Сталь", inStock: true, img: "https://example.com/balka_shvel.jpg", description: "Швеллер длиной 6 метров" },
            // ... 28 других балок ...
            
            // Строительные леса (30 позиций)
            { id: 301, name: "Леса строительные 10м", category: "Леса", price: 64200, material: "Сталь", inStock: true, img: "https://images.unsplash.com/photo-1581093458799-ef7c5c8b3c1e?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80", description: "Строительные леса высотой 10 метров" },
            // ... 29 других видов лесов ...
            
            // Ограждения (30 позиций)
            { id: 401, name: "Ограждение лестничное", category: "Ограждения", price: 5400, material: "Нержавейка", inStock: true, img: "https://example.com/ograjdenie.jpg", description: "Лестничное ограждение из нержавеющей стали" },
            // ... 29 других ограждений ...
            
            // Ворота (20 позиций)
            { id: 501, name: "Ворота распашные", category: "Ворота", price: 38200, material: "Сталь", inStock: true, img: "https://example.com/vorota.jpg", description: "Распашные ворота с калиткой" },
            // ... 19 других ворот ...
            
            // Навесы (20 позиций)
            { id: 601, name: "Навес автомобильный", category: "Навесы", price: 78500, material: "Сталь", inStock: true, img: "https://example.com/naves.jpg", description: "Автомобильный навес на два места" },
            // ... 19 других навесов ...
            
            // Лестницы (20 позиций)
            { id: 701, name: "Лестница металлическая", category: "Лестницы", price: 28700, material: "Сталь", inStock: true, img: "https://example.com/lestnica.jpg", description: "Металлическая лестница с перилами" },
            // ... 19 других лестниц ...
            
            // Емкости (20 позиций)
            { id: 801, name: "Емкость для воды 5м3", category: "Емкости", price: 64500, material: "Нержавейка", inStock: false, img: "https://example.com/emkost.jpg", description: "Емкость для воды объемом 5 кубометров" }
            // ... 19 других емкостей ...
        ];

        // Генерация категорий и материалов для фильтров
        const categories = [...new Set(productsDB.map(p => p.category))];
        const materials = [...new Set(productsDB.map(p => p.material))];
        
        // DOM элементы
        const loginBtn = document.getElementById('login-btn');
        const cartBtn = document.getElementById('cart-btn');
        const requestBtn = document.getElementById('request-btn');
        const loginModal = document.getElementById('login-modal');
        const cartModal = document.getElementById('cart-modal');
        const closeModalBtns = document.querySelectorAll('.close-modal');
        const productsContainer = document.getElementById('products-container');
        const categoryFilters = document.getElementById('category-filters');
        const materialFilters = document.getElementById('material-filters');
        const applyFiltersBtn = document.getElementById('apply-filters');
        const sortSelect = document.getElementById('sort-products');
        const cartItems = document.getElementById('cart-items');
        const cartTotalPrice = document.getElementById('cart-total-price');
        const cartCount = document.querySelector('.cart-count');
        
        // Корзина
        let cart = JSON.parse(localStorage.getItem('cart')) || [];
        
        // Инициализация магазина
        function initStore() {
            renderCategoryFilters();
            renderMaterialFilters();
            renderProducts(productsDB);
            updateCart();
        }
        
        // Рендер категорий фильтров
        function renderCategoryFilters() {
            categories.forEach(category => {
                const option = document.createElement('div');
                option.className = 'filter-option';
                option.innerHTML = `
                    <input type="checkbox" id="cat-${category}" value="${category}">
                    <label for="cat-${category}">${category}</label>
                `;
                categoryFilters.appendChild(option);
            });
        }
        
        // Рендер материалов фильтров
        function renderMaterialFilters() {
            materials.forEach(material => {
                const option = document.createElement('div');
                option.className = 'filter-option';
                option.innerHTML = `
                    <input type="checkbox" id="mat-${material}" value="${material}">
                    <label for="mat-${material}">${material}</label>
                `;
                materialFilters.appendChild(option);
            });
        }
        
        // Рендер продуктов
        function renderProducts(products) {
            productsContainer.innerHTML = '';
            
            if (products.length === 0) {
                productsContainer.innerHTML = '<p class="empty-catalog">Товары не найдены</p>';
                return;
            }
            
            products.forEach(product => {
                const productCard = document.createElement('div');
                productCard.className = 'product-card';
                
                let badge = '';
                if (!product.inStock) {
                    badge = '<div class="product-badge">Под заказ</div>';
                } else if (product.price < 20000) {
                    badge = '<div class="product-badge">Хит</div>';
                }
                
                productCard.innerHTML = `
                    ${badge}
                    <div class="product-img">
                        <img src="${product.img}" alt="${product.name}">
                    </div>
                    <div class="product-content">
                        <h3 class="product-title">${product.name}</h3>
                        <div class="product-meta">
                            <span>${product.category}</span>
                            <span>${product.material}</span>
                        </div>
                        <div class="product-price">${product.price.toLocaleString()} ₽</div>
                        <div class="product-actions">
                            <button class="btn-add-to-cart" data-id="${product.id}" data-name="${product.name}" data-price="${product.price}" data-img="${product.img}">
                                <i class="fas fa-shopping-cart"></i> В корзину
                            </button>
                            <button class="btn-view">
                                <i class="fas fa-eye"></i> Подробнее
                            </button>
                        </div>
                    </div>
                `;
                
                productsContainer.appendChild(productCard);
            });
            
            // Добавляем обработчики для кнопок корзины
            document.querySelectorAll('.btn-add-to-cart').forEach(btn => {
                btn.addEventListener('click', addToCartHandler);
            });
        }
        
        // Обработчик добавления в корзину
        function addToCartHandler(e) {
            const id = e.target.closest('.btn-add-to-cart').dataset.id;
            const name = e.target.closest('.btn-add-to-cart').dataset.name;
            const price = parseInt(e.target.closest('.btn-add-to-cart').dataset.price);
            const img = e.target.closest('.btn-add-to-cart').dataset.img;
            
            // Проверяем, есть ли товар уже в корзине
            const existingItem = cart.find(item => item.id === id);
            
            if (existingItem) {
                existingItem.quantity++;
            } else {
                cart.push({
                    id,
                    name,
                    price,
                    img,
                    quantity: 1
                });
            }
            
            updateCart();
            
            // Показываем уведомление
            showNotification('Товар добавлен в корзину!');
        }
        
        // Обновить корзину
        function updateCart() {
            // Сохраняем корзину в localStorage
            localStorage.setItem('cart', JSON.stringify(cart));
            
            // Обновляем счетчик товаров
            cartCount.textContent = cart.reduce((total, item) => total + item.quantity, 0);
            
            // Очищаем контейнер
            cartItems.innerHTML = '';
            
            // Если корзина пуста
            if (cart.length === 0) {
                cartItems.innerHTML = '<p class="empty-cart">Ваша корзина пуста</p>';
                cartTotalPrice.textContent = '0 ₽';
                return;
            }
            
            // Добавляем товары
            let total = 0;
            cart.forEach((item, index) => {
                const itemTotal = item.price * item.quantity;
                total += itemTotal;
                
                const cartItemEl = document.createElement('div');
                cartItemEl.className = 'cart-item';
                cartItemEl.innerHTML = `
                    <div class="cart-item-img">
                        <img src="${item.img}" alt="${item.name}">
                    </div>
                    <div class="cart-item-details">
                        <div class="cart-item-title">${item.name}</div>
                        <div class="cart-item-price">${item.price.toLocaleString()} ₽</div>
                        <div class="cart-item-actions">
                            <div class="quantity-control">
                                <button class="quantity-btn minus" data-index="${index}">-</button>
                                <input type="text" class="quantity-input" value="${item.quantity}" readonly>
                                <button class="quantity-btn plus" data-index="${index}">+</button>
                            </div>
                            <button class="remove-item" data-index="${index}">
                                <i class="fas fa-trash"></i>
                            </button>
                        </div>
                    </div>
                `;
                
                cartItems.appendChild(cartItemEl);
            });
            
            // Обновляем итоговую сумму
            cartTotalPrice.textContent = `${total.toLocaleString()} ₽`;
            
            // Добавляем обработчики для кнопок изменения количества
            document.querySelectorAll('.quantity-btn.minus').forEach(btn => {
                btn.addEventListener('click', () => {
                    const index = parseInt(btn.dataset.index);
                    if (cart[index].quantity > 1) {
                        cart[index].quantity--;
                        updateCart();
                    }
                });
            });
            
            document.querySelectorAll('.quantity-btn.plus').forEach(btn => {
                btn.addEventListener('click', () => {
                    const index = parseInt(btn.dataset.index);
                    cart[index].quantity++;
                    updateCart();
                });
            });
            
            // Добавляем обработчики для кнопок удаления
            document.querySelectorAll('.remove-item').forEach(btn => {
                btn.addEventListener('click', () => {
                    const index = parseInt(btn.dataset.index);
                    cart.splice(index, 1);
                    updateCart();
                });
            });
        }
        
        // Показать уведомление
        function showNotification(message) {
            const notification = document.createElement('div');
            notification.className = 'notification';
            notification.textContent = message;
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.classList.add('show');
            }, 10);
            
            setTimeout(() => {
                notification.classList.remove('show');
                setTimeout(() => {
                    document.body.removeChild(notification);
                }, 300);
            }, 3000);
        }
        
        // Обработчики событий
        loginBtn.addEventListener('click', () => {
            loginModal.classList.add('active');
            document.body.style.overflow = 'hidden';
        });
        
        cartBtn.addEventListener('click', () => {
            cartModal.classList.add('active');
            document.body.style.overflow = 'hidden';
        });
        
        requestBtn.addEventListener('click', () => {
            document.getElementById('request').scrollIntoView({ behavior: 'smooth' });
        });
        
        closeModalBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                loginModal.classList.remove('active');
                cartModal.classList.remove('active');
                document.body.style.overflow = '';
            });
        });
        
        window.addEventListener('click', (e) => {
            if (e.target === loginModal || e.target === cartModal) {
                loginModal.classList.remove('active');
                cartModal.classList.remove('active');
                document.body.style.overflow = '';
            }
        });
        
        // Применение фильтров
        applyFiltersBtn.addEventListener('click', applyFilters);
        
        function applyFilters() {
            // Получаем выбранные категории
            const selectedCategories = Array.from(document.querySelectorAll('#category-filters input:checked'))
                .map(input => input.value);
            
            // Получаем выбранные материалы
            const selectedMaterials = Array.from(document.querySelectorAll('#material-filters input:checked'))
                .map(input => input.value);
            
            // Фильтруем товары
            let filteredProducts = productsDB;
            
            if (selectedCategories.length > 0) {
                filteredProducts = filteredProducts.filter(p => selectedCategories.includes(p.category));
            }
            
            if (selectedMaterials.length > 0) {
                filteredProducts = filteredProducts.filter(p => selectedMaterials.includes(p.material));
            }
            
            // Фильтр по наличию
            const inStock = document.getElementById('in-stock').checked;
            const preOrder = document.getElementById('pre-order').checked;
            
            if (inStock && !preOrder) {
                filteredProducts = filteredProducts.filter(p => p.inStock);
            } else if (!inStock && preOrder) {
                filteredProducts = filteredProducts.filter(p => !p.inStock);
            }
            
            // Сортировка
            const sortValue = sortSelect.value;
            if (sortValue === 'price-asc') {
                filteredProducts.sort((a, b) => a.price - b.price);
            } else if (sortValue === 'price-desc') {
                filteredProducts.sort((a, b) => b.price - a.price);
            }
            
            renderProducts(filteredProducts);
        }
        
        // Стили для уведомлений
        const notificationStyle = document.createElement('style');
        notificationStyle.textContent = `
            .notification {
                position: fixed;
                bottom: 30px;
                right: 30px;
                background: var(--success);
                color: white;
                padding: 20px 30px;
                border-radius: 8px;
                box-shadow: 0 10px 25px rgba(0,0,0,0.2);
                transform: translateY(100px);
                opacity: 0;
                transition: transform 0.4s ease, opacity 0.4s ease;
                z-index: 3000;
                font-size: 18px;
                font-weight: 500;
            }
            
            .notification.show {
                transform: translateY(0);
                opacity: 1;
            }
        `;
        document.head.appendChild(notificationStyle);
        
        // Инициализация при загрузке
        document.addEventListener('DOMContentLoaded', () => {
            initStore();
        });
    </script>
</body>
    </html>

